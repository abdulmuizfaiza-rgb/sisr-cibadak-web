[build]
  publish = "public"
  functions = "netlify/functions"

# Semua permintaan ke /api/* diarahkan ke fungsi serverless "api" (lihat netlify/functions/api.js)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true
